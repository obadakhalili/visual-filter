{
  "name": "@visual-filter/vue2",
  "version": "0.9.0",
  "scripts": {
    "dev": "vite",
    "dev:rebuild": "vite --force",
    "format": "yarn run prettier --ignore-path ../../.gitignore --write",
    "build": "rollup --config ../../rollup.config.js --environment NODE_ENV:production,package:vue2",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@visual-filter/applyer": "0.9.0",
    "@visual-filter/common": "0.9.0"
  },
  "devDependencies": {
    "prettier": "^2.2.1",
    "rollup": "^2.44.0",
    "vite": "^2.1.5",
    "vite-plugin-vue2": "^1.4.3",
    "vue": "^2.6.12"
  }
}

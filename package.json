{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "format": "yarn run prettier --ignore-path .gitignore --write ."
  },
  "devDependencies": {
    "prettier": "^2.2.1",
    "typescript": "^4.2.3",
    "vite": "^2.1.5"
  }
}

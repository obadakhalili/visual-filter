{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "format": "yarn run prettier --write .",
    "format:all": "yarn run prettier --ignore-path .gitignore --write ."
  },
  "devDependencies": {
    "postcss": "^8.2.9",
    "prettier": "^2.2.1",
    "tailwindcss": "^2.1.1",
    "typescript": "^4.2.3",
    "vite": "^2.1.5"
  }
}
